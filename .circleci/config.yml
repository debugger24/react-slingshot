version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: "npn install"
      - run: "npn build"
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: "npn install"
      - run: "npn test"
  analyze:
    docker:
        - image: circleci/node:latest
    steps:
      - checkout
      - run: "npn install"
      - run: "npn build"
      - run: "analyze-bundle"

workflows:
  version: 2
  first_workflow:
    jobs:
      - build
      - test
      - analyze
